// The single source for the app's display name — the wireframes' `navbar`
// title. Imported wherever the brand needs to appear (the shell, App.tsx,
// NoAccess) rather than restated.
export const APP_NAME = "Todo App";
